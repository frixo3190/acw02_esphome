text:
  - platform: template
    name: "MQTT: Broker"
    id: mqtt_broker_address_input
    entity_category: config
    update_interval: 60s
    mode: text
    lambda: |-
      return std::string(id(ac_ctrl).get_mqtt_broker());
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mqtt_broker(x);
            id(mqtt_broker_address_input).update();

  - platform: template
    name: "MQTT: Login"
    id: mqtt_login_input
    entity_category: config
    update_interval: 60s
    mode: text
    lambda: |-
      return std::string(id(ac_ctrl).get_mqtt_username());
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mqtt_username(x);
            id(mqtt_login_input).update();

  - platform: template
    name: "MQTT: Password"
    id: mqtt_password_input
    entity_category: config
    update_interval: 60s
    mode: password
    lambda: |-
      return std::string(id(ac_ctrl).get_mqtt_password());
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mqtt_password(x);
            id(mqtt_password_input).update();

  - platform: template
    name: "MQTT: Port"
    id: mqtt_port_input
    update_interval: 60s
    mode: text
    entity_category: config
    lambda: |-
      return std::string(id(ac_ctrl).int_to_string(id(ac_ctrl).get_mqtt_port()));
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mqtt_port_from_string(x);
            id(mqtt_port_input).update();

  - platform: template
    name: "G1: Z-CMD Delai cmd silencieuse"
    id: mute_next_cmd_delay
    update_interval: 60s
    icon: mdi:volume-off
    mode: text
    entity_category: config
    lambda: |-
      return std::string(id(ac_ctrl).int_to_string(id(ac_ctrl).get_mute_next_cmd_delay()));
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mute_next_cmd_delay_string(x);
  - platform: template
    name: "G1: Z-CMD Delai cmd silencieuse après un allumage"
    id: mute_next_cmd_after_on_delay
    update_interval: 60s
    icon: mdi:volume-off
    mode: text
    entity_category: config
    lambda: |-
      return std::string(id(ac_ctrl).int_to_string(id(ac_ctrl).get_mute_next_cmd_after_on_delay()));
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_mute_next_cmd_after_on_delay_string(x);

  - platform: template
    name: "G1: Z-CMD Delai de publication de status apres un allumage"
    id: publish_stats_after_power_on_delay
    update_interval: 60s
    icon: mdi:publish
    mode: text
    entity_category: config
    lambda: |-
      return std::string(id(ac_ctrl).int_to_string(id(ac_ctrl).get_publish_stats_after_power_on_delay()));
    set_action:
      then:
        - lambda: |-
            id(ac_ctrl).set_publish_stats_after_power_on_delay_string(x);

switch:
  - platform: template
    name: "G1: Options version MQTT"
    id: g1_mqtt_options
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_g1_mqtt_options();
    turn_on_action:
      - lambda: id(ac_ctrl).set_g1_mqtt_options(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_g1_mqtt_options(false);

  - platform: template
    name: "G1: Commande silencieuse"
    id: mute_commande_api
    icon: mdi:volume-off
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_mute_on();
    turn_on_action:
      - lambda: id(ac_ctrl).set_mute(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_mute(false);

  - platform: template
    name: "Z-Config: Exclu mode Auto"
    id: disable_mode_auto
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_mode_auto();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_mode_auto(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_mode_auto(false);

  - platform: template
    name: "Z-Config: Exclu mode Chauffage"
    id: disable_mode_heat
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_mode_heat();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_mode_heat(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_mode_heat(false);

  - platform: template
    name: "Z-Config: Exclu mode Déshumidification"
    id: disable_mode_dry
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_mode_dry();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_mode_dry(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_mode_dry(false);

  - platform: template
    name: "Z-Config: Exclu mode Ventilation"
    id: disable_mode_fan
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_mode_fan();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_mode_fan(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_mode_fan(false);

  - platform: template
    name: "Z-Config: Exclu oscillation vertical"
    id: disable_swing_vertical
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_swing_vertical();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_swing_vertical(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_swing_vertical(false);

  - platform: template
    name: "Z-Config: Exclu oscillation horizontal"
    id: disable_swing_horizontal
    icon: "mdi:thermostat-cog"
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_disable_swing_horizontal();
    turn_on_action:
      - lambda: id(ac_ctrl).set_disable_swing_horizontal(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_disable_swing_horizontal(false);

  - platform: template
    name: "G1: Auto calc climate (eco)"
    id: option_recalculate_climate
    icon: mdi:autorenew
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_option_recalculate_climate();
    turn_on_action:
      - lambda: id(ac_ctrl).set_option_recalculate_climate(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_option_recalculate_climate(false);

  - platform: template
    name: "G1: Mode éteint reset Eco/Purificateur"
    id: reset_eco_purifier_ac_off
    icon: mdi:lock-reset
    entity_category: "config"
    lambda: |-
      return id(ac_ctrl).is_auto_off_options_when_ac_off();
    turn_on_action:
      - lambda: id(ac_ctrl).set_auto_off_options_when_ac_off(true);
    turn_off_action:
      - lambda: id(ac_ctrl).set_auto_off_options_when_ac_off(false);

button:
  - platform: restart
    name: "G1: Redémarrer"
    id: restart_module_ac

  - platform: restart
    name: "MQTT: Valider"
    id: validate_mqtt
  
  - platform: template
    name: "G1: Actualiser data"
    id: get_status_button
    icon: mdi:cloud-refresh-variant
    entity_category: "config"
    on_press:
      - lambda: |-
          id(ac_ctrl).reload_ac_info();

  - platform: template
    name: "G1: Reconstruire les entitées MQTT"
    id: rebuild_mqtt_entities
    icon: mdi:cloud-refresh-variant
    entity_category: "config"
    on_press:
      - lambda: |-
          id(ac_ctrl).rebuild_mqtt_entity();
  
  - platform: template
    name: "Z-Config: Valider"
    id: apply_disable_settings
    icon: mdi:send
    entity_category: "config"
    on_press:
      - lambda: |-
          id(ac_ctrl).apply_disable_settings();

text_sensor:
  - platform: template
    name: "WiFi: mDNS"
    id: address_sensor
    entity_category: "diagnostic"
    lambda: |-
      return { id(ac_ctrl).get_address() };
    on_value:
      - lambda: |-
          id(ac_ctrl).entity_mdns_sync(id(address_sensor).state);
  - platform: wifi_info
    ip_address:
      name: "WiFi: IP"
      id: wifi__ip
      on_value:
       - lambda: |-
          id(ac_ctrl).entity_wifi_ip_sync(id(wifi__ip).state);
    ssid:
      name: "WiFi: Point d'accès connecté"
      id: wifi__ssid
      on_value:
       - lambda: |-
          id(ac_ctrl).entity_wifi_ssid_sync(id(wifi__ssid).state);
    bssid:
      name: "WiFi: BSSID"
      id: wifi__bssid
      on_value:
       - lambda: |-
          id(ac_ctrl).entity_wifi_bssid_sync(id(wifi__bssid).state);
    mac_address:
      name: "WiFi: MAC"
      id: wifi__mac
      on_value:
       - lambda: |-
          id(ac_ctrl).entity_wifi_mac_sync(id(wifi__mac).state);
  - platform: version
    name: "ESP: Version"
    id: esp__version
    on_value:
       - lambda: |-
          id(ac_ctrl).entity_esp_version_sync(id(esp__version).state);
  - platform: template
    name: "Warn Info"
    icon: mdi:alert-circle-outline
    id: ac_warn_text
    internal: true
  - platform: template
    name: "Error Info"
    icon: mdi:alert-octagon-outline
    id: ac_error_text
    internal: true

sensor:
  - platform: wifi_signal
    name: "WiFi: Signal"
    id: wifi__signal
    update_interval: 20s
    on_value:
      - lambda: |-
          char buf[16];
          snprintf(buf, sizeof(buf), "%.0f", x);
          id(ac_ctrl).entity_wifi_signal_sync(std::string(buf));
  - platform: internal_temperature
    name: "ESP: Température CPU"
    id: esp__temp
    update_interval: 20s
    on_value:
      - lambda: |-
          char buf[16];
          snprintf(buf, sizeof(buf), "%.1f", x);
          id(ac_ctrl).entity_internal_temperature_sync(std::string(buf));
  - platform: template
    name: "ESP: Mémoire libre"
    id: esp__memory
    lambda: |-
      return heap_caps_get_free_size(MALLOC_CAP_INTERNAL) / 1024.0;
    unit_of_measurement: "kB"
    update_interval: 20s
    entity_category: "diagnostic"
    on_value:
      - lambda: |-
          char buf[16];
          snprintf(buf, sizeof(buf), "%.1f", x);
          id(ac_ctrl).entity_esp_memory_sync(std::string(buf));

binary_sensor:
  - platform: template
    id: mqtt_conn_status
    name: "MQTT"
    device_class: connectivity
    entity_category: "diagnostic"
  - platform: template
    id: filter_dirty
    name: "Filtre à nettoyer"
    device_class: problem
    internal: true
  - platform: template
    id: warn_sensor
    name: "Warn"
    device_class: problem
    internal: true
  - platform: template
    id: error_sensor
    name: "Error"
    device_class: problem
    internal: true

globals:
  - id: stop_mqtt_updates
    type: bool
    restore_value: no
    initial_value: 'false'

interval:
  - interval: 1s
    then:
      lambda: |-
        id(ac_ctrl).set_mqtt_connected_sensor(id(mqtt_conn_status));
        id(ac_ctrl).set_filter_dirty_sensor(id(filter_dirty));
        id(ac_ctrl).set_warn_sensor(id(warn_sensor));
        id(ac_ctrl).set_error_sensor(id(error_sensor));
        id(ac_ctrl).set_warn_text_sensor(id(ac_warn_text));
        id(ac_ctrl).set_error_text_sensor(id(ac_error_text));
        id(ac_ctrl).set_mute_next_cmd_delay_text(id(mute_next_cmd_delay));
        id(ac_ctrl).set_mute_next_cmd_after_on_delay_text(id(mute_next_cmd_after_on_delay));
        id(ac_ctrl).set_publish_stats_after_power_on_delay_text(id(publish_stats_after_power_on_delay));
  - interval: 2s
    then:
      - lambda: |-
          if (id(stop_mqtt_updates)) return;
          id(mqtt_broker_address_input).update();
          id(mqtt_login_input).update();
          id(mqtt_password_input).update();
          id(mqtt_port_input).update();
          id(mute_next_cmd_delay).update();
          id(mute_next_cmd_after_on_delay).update();
          id(publish_stats_after_power_on_delay).update();
          id(address_sensor).update();
          id(wifi__ip).update();
          id(wifi__ssid).update();
          id(wifi__bssid).update();
          id(wifi__signal).update();
          id(esp__temp).update();
          id(esp__memory).update();
  - interval: 40s
    then:
      - lambda: |-
          id(stop_mqtt_updates) = true;
